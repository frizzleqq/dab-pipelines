# Orchestration job for the machine data pipeline.

resources:
  jobs:
    machine_data_orchestration:
      name: machine_data_orchestration

      tasks:
        - task_key: run_pipeline
          pipeline_task:
            pipeline_id: ${resources.pipelines.pipeline_machine_data.id}
